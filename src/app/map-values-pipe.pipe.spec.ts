import { MapValuesPipePipe } from './map-values-pipe.pipe';

describe('MapValuesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MapValuesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
